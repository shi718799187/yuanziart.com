# 圆子插画师站 · 部署说明

> **当前状态：已上线**
> 正式地址：<https://yuanziart.com>（`www.yuanziart.com` 同样可访问）
> 托管方式：Cloudflare Workers 静态资源 ｜ 源码仓库：GitHub `shi718799187/yuanziart.com`
> 备案：**不需要**（服务器在 Cloudflare，境外）

本说明分六部分：

1. 架构与日常流程 ← **平时只用看这个**
2. Cloudflare 侧配置（改配置时照抄）
3. 开发机环境坑（换电脑 / 重装必看）
4. 目录与命令速查
5. 备选托管方案（CloudBase / Gitee）
6. 费用与备案

---

## 一、架构与日常流程

```
content/site-data.js          ← 全站文案的唯一来源
        │
        │  ① 推送到 GitHub
        ▼
GitHub: shi718799187/yuanziart.com
        │
        │  ② Cloudflare 收到推送，云端自动执行 node build.js
        ▼
      dist/                   ← 构建产物（不进版本库，见 .gitignore）
        │
        │  ③ npx wrangler deploy 上传静态资源
        ▼
Cloudflare Workers  ──►  https://yuanziart.com
```

**核心：`dist/` 不进版本库。** 推送到 GitHub 后，Cloudflare 在云端现场构建再上传。
所以 **推送 = 上线**，大约 1 分钟生效。没有数据库、不用迁移、不会停机。

### 日常改内容（唯一要做的事）

1. 改 `content/site-data.js`
2. 如果引入了**原本没出现过的汉字** → 再跑一次 `node build-fonts.js`
3. 提交并推送：

```bash
node build.js
git add -A
git commit -m "更新文案"
git -c http.proxy=http://127.0.0.1:7890 -c https.proxy=http://127.0.0.1:7890 push
```

4. 等约 1 分钟，刷新线上页面

> 推送命令里那两个 `-c http.proxy=...` 是**必须的**，原因见第三部分。

本地想看效果：`node preview.js` → 浏览器打开 <http://127.0.0.1:8080>

---

## 二、Cloudflare 侧配置（改配置时照抄）

### 2.1 仓库里的 `wrangler.jsonc`

Cloudflare 靠这个文件知道"静态文件在哪"。**改它要和控制台保持同步**：

```jsonc
{
  "name": "yuanziart-com",            // ⚠️ 必须和控制台里的项目名一字不差
  "compatibility_date": "2026-09-17",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "404-page"  // 未知路径回退到 dist/404.html
  }
}
```

- `name` 不一致的话，`wrangler deploy` 会去创建/更新**另一个同名 Worker**，两边对不上
- `name` 只允许字母、数字、连字符（不能有下划线或点）

### 2.2 控制台里的构建设置

路径：**Workers & Pages → `yuanziart-com` → Settings → Build**

| 字段 | 值 |
|---|---|
| Production branch | `main` |
| Framework preset | `None` |
| Build command | `node build.js` |
| Deploy command | `npx wrangler deploy` |
| Non-production branch deploy command | `npx wrangler versions upload`（默认） |
| Path | `/`（默认） |
| 环境变量 | 名字 `NODE_VERSION`，值 `20` |

> ⚠️ **绝对不要在云端跑 `build-fonts.js`。**
> 它需要联网访问 `fonts.googleapis.com` 并**覆盖**字体文件 —— 放云端会让部署依赖外网。
> 字体（`assets/fonts/*.woff2` + `assets/css/fonts.css`）是**提交进仓库**的，
> 改完文案在本机重跑一次，把结果一起提交即可。

### 2.3 自定义域名

- `yuanziart.com` 和 `www.yuanziart.com` 都指向同一个 Worker
- 添加位置：**Workers & Pages → `yuanziart-com` → Settings → Domains & Routes → Add Domain**
- 域名的 DNS 托管在 Cloudflare（NS 为 `raquel.ns.cloudflare.com` / `valentin.ns.cloudflare.com`）
- HTTPS 证书由 Cloudflare 自动签发，免费
- 站点的 `canonical` 用的是**根域名**，建议加一条 Redirect Rule 把 `www` 永久跳到根域名

### 2.4 ⚠️ URL 形式：无扩展名（易踩）

Cloudflare 静态资源默认会把 `/about.html` **307 规范化成 `/about`**（去掉扩展名）。

因此 `canonical`、`og:url`、`sitemap.xml` 用的都是**无扩展名地址**，与最终 URL 保持一致。
改 `build.js` 时注意 `urlPath()` 这个函数 —— 它统一负责这件事，别绕过它。

（现状：站内链接仍带 `.html`，访问时会有一次 307 跳转到无扩展名地址。功能正常，
只是多一次边缘内的往返。要让站内链接也变成无扩展名，除了改模板/文案里的 href，
还必须同时改 `preview.js`，否则本地预览点链接会 404。）

---

## 三、开发机环境坑（换电脑 / 重装必看）

### 3.1 必须走本地代理才能访问 GitHub

这台机器通过本地代理 **`127.0.0.1:7890`** 上网（Clash / v2ray 这类客户端），
代理写在 Windows 的 Internet 设置里（注册表 `HKCU\...\Internet Settings`）。

- 浏览器、`.NET`、`Invoke-WebRequest` **会自动走代理**
- **git 和 curl 不读这个设置**，会直连 GitHub → 被重置

典型报错：

```
fatal: unable to access 'https://github.com/...': Recv failure: Connection was reset
```

**解决**：每次 push 都带代理参数

```bash
git -c http.proxy=http://127.0.0.1:7890 -c https.proxy=http://127.0.0.1:7890 push
```

想一劳永逸（执行一次，永久生效）：

```bash
git config --global http.proxy http://127.0.0.1:7890
```

**排查思路**（下次遇到网络问题可复用）：
1. `Test-NetConnection` / `TcpClient` 测端口通不通
2. 查系统代理：`netsh winhttp show proxy` + 注册表 `ProxyEnable` / `ProxyServer`
3. 用 `.NET`（`Invoke-WebRequest`）和 `curl` 分别测同一个地址 —— 若前者通、后者不通，基本就是代理问题

### 3.2 `*.workers.dev` 在这个网络不可用

Cloudflare 会送一个测试地址，形如 `yuanziart-com.<账号子域>.workers.dev`。
**在本地这个域名被 DNS 污染**（解析到非 Cloudflare 的 IP `173.231.12.107`），
直连超时、走代理 TLS 握手失败。公网 DoH 查询甚至返回 NXDOMAIN。

**这不影响正式站点** —— 自定义域名走的是 Cloudflare 常规 IP 段，正常访问。
排查线上问题时**不要用 `*.workers.dev` 地址**，直接测 `https://yuanziart.com`。

### 3.3 `Get-Content` 需要显式编码

本机的命令安全策略会拦截不带编码的 `Get-Content`（防止 GBK/UTF-8 误判损坏文件）。
读文件要写成 `Get-Content xxx -Encoding utf8`，或者干脆用编辑工具而不是 shell。

### 3.4 Node 不在 PATH 上

Node 装在 `.workbuddy` 下，命令要写全路径：

```
C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-3\node.exe
```

或者自己装一个 Node 18+（`build-fonts.js` 用了内置 `fetch`，不能低于 18）。

---

## 四、目录与命令速查

### 目录都是干什么的

| 路径 | 说明 |
|---|---|
| `content/site-data.js` | **全站文案的唯一来源。** 改文字只改这一个文件 |
| `templates/` | 页面结构（含 `_partials/head.html`、`nav.html`、`footer.html`） |
| `assets/css/style.css` | 设计系统 + 全部样式 |
| `assets/js/` | `main.js`（交互）、`filmstrip.js`（首页作品长廊）、`planes.js`（星星装饰）、`analytics.js` |
| `assets/images/` | 作品图，**双格式**：`.webp`（主）+ `.jpg`（回退）。文件名不带扩展名地写在 `site-data.js` 里 |
| `assets/fonts/` + `assets/css/fonts.css` | **自托管字体，由脚本生成，不要手改** |
| `lab/filmstrip.html` | 作品长廊的独立实验页（带速度/缩放滑杆，`noindex`） |
| `build.js` | 构建：注入文案 → 输出到 `dist/` |
| `build-fonts.js` | 生成字体子集（只在文案/页面变化后需要重跑） |
| `preview.js` | 本地预览用的零依赖静态服务器（服务 `dist/`） |
| `wrangler.jsonc` | Cloudflare 部署配置（静态资源目录 / 404 回退） |
| `netlify.toml` | 早期备选方案留下的，Cloudflare 不读它，可忽略 |
| `dist/` | **构建产物。** 全部由构建生成，不要手改，也不进版本库 |

### 三条命令

```bash
node build.js          # 改完文案 / 模板 / 样式 → 重建 dist
node build-fonts.js    # 改了文案、或新增了页面之后 → 重做字体子集
node preview.js        # 本地预览，浏览器打开 http://127.0.0.1:8080
```

日常只需要第一条。**第二条什么时候必须跑**：新增了原本没出现过的汉字。
不跑不会坏（那些字退回系统字体），但字重/字宽会和设计稿不一致。

### 字体为什么是自托管的

站内用的 Noto Sans SC / Noto Serif SC / Ma Shan Zheng 都是 CJK 字体。
Google Fonts 在线引用会把它们切成上百个 `unicode-range` 分片，首屏要发几十个请求，
而且 `fonts.googleapis.com` 在大陆访问不稳定 —— 用户实际看到的是苹方/雅黑回落版。

现在用 `text=` 子集接口只取「本站实际用到的字符」，7 个 woff2 共约 720KB。

### 图片规范

每张作品图需要 **两种格式同名文件**（`.webp` + `.jpg`），
`<picture>` 里 WebP 优先、JPEG 回退。出图时：

- 最长边压到 1600px（站内最大显示宽度只有 860px 的灯箱）
- 裁掉源图自带的纯白留白（否则页面上会出现"白边"，改 CSS 治不了）
- 交付体积参考：8 张作品 = WebP 约 2MB + JPEG 约 2MB

---

## 五、备选托管方案（当前未使用，留档）

### 方案 A：腾讯云 CloudBase 静态托管

1. 控制台 → **云开发 CloudBase** → 新建环境（选「按量计费/免费额度」档）
   —— **不要和小程序共用同一环境**
2. 进入新环境 → **静态网站托管** → 开通
3. 上传 `dist/` 全部内容（或 `npx @cloudbase/cli deploy dist -e 环境ID`）
4. 「设置 → 自定义域名」绑定域名（**国内节点需先完成 ICP 备案**）

> 免费额度：每环境 1GB 存储 + 5GB 流量/月。
> 404 页面：环境「静态网站托管 → 404 页面」选 `404.html`。

### 方案 B：Gitee Pages

1. gitee.com 新建仓库，把 `dist/` 内容推上去（注意别被 `.gitignore` 忽略掉）
2. 仓库 → **服务** → **Gitee Pages** → 部署目录填 `/` → 启动
3. 得到 `https://<用户名>.gitee.io/<仓库名>`

> 适合预算优先、流量不大；更新需重新推送。

### 本机预览

`preview.js` 已对未知路径回退到 `dist/404.html`，行为与线上一致。

---

## 六、费用与备案

| 项目 | 费用 |
|---|---|
| 域名（腾讯云购买） | ¥35 – 120 / 年 |
| Cloudflare Workers + 静态资源 | ¥0（免费额度足够） |
| HTTPS 证书 | ¥0（Cloudflare 自动签发） |
| ICP 备案 | **不需要**（服务器在境外） |

合计约 **¥35 – 120 / 年**。

> 如果将来改用**国内服务器**（腾讯云 / 阿里云等），就必须做 ICP 备案
> （免费，约 2–3 周，需身份证实名）。

---

## 七、待办 / 已知未完成

| 事项 | 说明 |
|---|---|
| 示例内容替换 | 首页与关于页的数字带、学员评价、课程价格、时间线目前仍是示例，页面上可见「示例」字样 |
| 微信二维码 | `assets/images/wechat-qr.png` 尚未上传，弹层显示「建设中」占位 —— **这是唯一的转化入口，建议优先补** |
| 授课平台链接 | `content/site-data.js` 里 `contact.platform.url` 为空，页脚只显示平台名不可点 |
| 统计埋点 | `templates/_partials/head.html` 判断的是 `site.analytics.domain`，但数据在顶层的 `analytics.domain`，**路径不匹配**，导致统计脚本永远不加载；且默认接入的 Plausible 是收费服务。要接统计需先定工具（Umami 免费版 / GA4） |
| `www → 根域名` 跳转 | 两个地址都能访问、内容相同，建议加 Redirect Rule 统一 |
| 站内链接无扩展名化 | 可选优化，见 2.4 |
