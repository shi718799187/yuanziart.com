# 圆子插画师站 · 免费部署说明

本说明针对「纯静态站点、零服务器、尽量不花钱」的上线路线。
站点已通过 `build.js` 构建为 `dist/` 目录，里面是可直接托管的纯静态文件。

---

## 零、换一台电脑怎么接着做

### 需要什么
只要 **Node.js 18 或更高**（`build-fonts.js` 用了内置 `fetch`，所以不能低于 18）。
**没有任何第三方依赖，不用 `npm install`** —— 项目里没有 `package.json`，两个构建脚本只用 Node 内置模块。

### 目录都是干什么的

| 路径 | 说明 |
|---|---|
| `content/site-data.js` | **全站文案的唯一来源。** 改文字只改这一个文件 |
| `templates/` | 页面结构（含 `_partials/head.html`、`nav.html`、`footer.html`） |
| `assets/css/style.css` | 设计系统 + 全部样式 |
| `assets/js/` | `main.js`（交互）、`filmstrip.js`（首页作品长廊）、`planes.js`（星星装饰）、`analytics.js` |
| `assets/fonts/` + `assets/css/fonts.css` | **自托管字体，由脚本生成，不要手改** |
| `lab/filmstrip.html` | 作品长廊的独立实验页（带速度/缩放滑杆，`noindex`） |
| `build.js` | 构建：注入文案 → 输出到 `dist/` |
| `build-fonts.js` | 生成字体子集（只在文案/页面变化后需要重跑） |
| `preview.js` | 本地预览用的零依赖静态服务器（服务 `dist/`，无第三方依赖） |
| `dist/` | **构建产物，部署的就是它。** 全部由构建生成，不要手改 |

### 三条命令

```bash
node build.js          # 改完文案 / 模板 / 样式 → 重建 dist
node build-fonts.js    # 改了文案、或新增了页面之后 → 重做字体子集
node preview.js        # 本地预览，浏览器打开 http://127.0.0.1:8080
```

日常只跑第一条。**第二条什么时候必须跑**：新增了原本没出现过的汉字。
不跑不会坏（那些字退回系统字体），但字重/字宽会和设计稿不一致。

### 字体为什么是自托管的
站内用的 Noto Sans SC / Noto Serif SC / Ma Shan Zheng 都是 CJK 字体。
Google Fonts 在线引用会把它们切成上百个 `unicode-range` 分片，首屏要发几十个请求，
而且 `fonts.googleapis.com` 在大陆访问不稳定 —— 用户实际看到的是苹方/雅黑回落版。
现在用 `text=` 子集接口只取「本站实际用到的字符」，7 个 woff2 共约 680KB。

---

## 一、两种免费托管方案（任选其一）

### 方案 A：腾讯云 CloudBase 静态托管（推荐，复用你已有账号）
你已有腾讯云个人账号，且小程序后台就在 CloudBase 里。
**不要和小程序共用同一环境**——新建一个独立环境（如 `yuanzi-portfolio`），互不干扰，还能再拿一份免费额度。

步骤：
1. 控制台 → **云开发 CloudBase** → 新建环境（选「按量计费/免费额度」档，无需买套餐）。
2. 进入新环境 → **静态网站托管** → 开通。
3. 把本地 `dist/` 目录全部上传（或用 CLI：`npx @cloudbase/cli deploy dist -e 环境ID`）。
4. 在「设置 → 自定义域名」里绑定你买的域名（绑定前需先完成 ICP 备案）。
5. 不设自定义域名也能用系统分配的 `*.cloudbaseapp.com` 临时访问。

> 免费额度：每环境 1GB 存储 + 5GB 流量/月，小作品集基本够用；超出按量（存储≈¥0.1/GB/月，CDN≈¥0.21/GB）。

### 方案 B：Gitee Pages（码云，国内访问稳、完全免费）
1. 在 gitee.com 新建仓库，把 `dist/` 内容推上去（注意 `.gitignore` 别把 `dist` 忽略掉）。
2. 仓库 → **服务** → **Gitee Pages** → 部署目录填 `/`（根目录）→ 启动。
3. 得到 `https://<用户名>.gitee.io/<仓库名>` 免费地址。
4. 想用自己域名：Gitee Pages 企业版/绑定自定义域名需实名与备案。

> Gitee Pages 免费版适合展示，更新需重新推送；适合预算优先、流量不大的情况。

---

## 二、上线前必做：填域名 → 自动生成 sitemap / robots

`robots.txt` 每次构建都会生成；`sitemap.xml` 需要绝对地址，所以**必须先在 `content/site-data.js` 里填好 `site.domain`**（结尾不要斜杠），例如：

```js
site: {
  // ...
  domain: 'https://yuanzi.art',   // 改成你的真实域名
}
```

然后重新构建：

```
node build.js     （或双击 build.bat）
```

构建日志会显示：
- `✓ robots.txt`              —— 始终生成
- `✓ sitemap.xml`             —— 填了域名后出现
- `✓ 404.html`                —— 始终生成

把新的 `dist/` 重新上传到托管平台即可。

---

## 三、404 页面

`templates/404.html` 已接入全站导航/页脚/统计，构建后位于 `dist/404.html`。
- **CloudBase**：环境「静态网站托管 → 404 页面」选择 `404.html`。
- **Gitee Pages**：根目录放 `404.html` 即自动作为错误页。
- **本机预览**：`preview.js` 已对未知路径回退到 `dist/404.html`。

---

## 四、备案与域名（国内路线必看）

- 国内买域名（.cn / .com / .art）+ 国内服务器托管 → **必须 ICP 备案**（免费，约 2–3 周，需身份证实名）。
- 一个备案号可挂同一账号下的多个环境/子域名，作品集环境不影响小程序备案。
- HTTPS 证书：腾讯云 / Let's Encrypt 提供**免费 DV 证书**，部署时开启即可。

---

## 五、费用回顾（年费）

| 项目 | 年费 |
|---|---|
| 域名（.cn / .com / .art） | ¥35 – 120 |
| 静态托管（CloudBase 免费额度内） | ¥0 |
| HTTPS 证书 | ¥0 |
| ICP 备案 | ¥0（耗时） |

合计约 **¥35 – 200 / 年**，十年不过千把块。

---

## 六、当前预览（临时）

本仓库已可一键部署到腾讯云 Cloud Studio 预览：
- 构建：`node build.js`
- 预览：`node preview.js`（静态服务 `dist/`）
- 注意：Cloud Studio 预览地址随沙箱重建会变，仅用于看效果，**不是正式上线地址**。
