# ============================================================
# 提交进仓库的是「源」，不是「产物」
# ------------------------------------------------------------
# 部署时 Cloudflare Pages 会在云端跑 `node build.js` 现场生成 dist/，
# 所以 dist/ 不进版本库 —— 否则每次提交都会带一大堆生成文件，
# 还得在仓库里出现「源改了但 dist 忘了重建」的不一致。
#
# 注意：assets/fonts/*.woff2 和 assets/css/fonts.css 虽然也是脚本生成的，
# 但它们是**提交**的 —— 因为 build-fonts.js 需要联网访问 fonts.googleapis.com，
# 放进云端构建会让部署依赖外网。改了文案后在本机重跑一次，把结果一起提交。
# ============================================================

# 构建产物
dist/

# 本地快照
_backup/

# 打包产物
*.zip

# IDE 元数据
.codebuddy/
.workbuddy/

# 系统杂项
Thumbs.db
Desktop.ini
.DS_Store
